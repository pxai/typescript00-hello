// define one variable
let greet: string = 'Hello';

// define another with implicit type:
let who = 'World';

console.log(greet + ' ' + who);

// string
let phrase: string = 'This is Typescript';

// number
let version: number = 5;
let weight: number = 78.6;

// boolean
let amIEvil: boolean = false;

// array
let names: string[] = ['Mobile','Console','Web'];

// any
let unknown : any;

console.log(greet + ' this is version ' + version);
console.log('Welcome to the ' + names[2]);
